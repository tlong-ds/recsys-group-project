apiVersion: v1
kind: Service
metadata:
  name: recsys-api
  namespace: recsys
  labels:
    app: recsys-api
spec:
  type: ClusterIP
  selector:
    app: recsys-api
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 8000
