global:
  scrape_interval: 15s

rule_files:
  - "/etc/prometheus/alerts.yml"

scrape_configs:
  - job_name: recsys-api
    bearer_token_file: /etc/prometheus/secrets/recsys-api-key
    static_configs:
      - targets: ["api:8000"]
